import { FadeIn } from '../ui/FadeIn'

/** Renders the four layer architecture recipe from registry snapshot to query answer. */
export function Architecture() {
  const layers = [['01', 'registry snapshot', 'curated npm metadata, versions, maintainers, publish times, bundled so the demo runs offline'], ['02', 'graph builder', 'streams nodes and edges into hydradb over bolt, idempotent merges, re-runnable'], ['03', 'hydradb', 'versioned package graph on object storage, traversals return in milliseconds'], ['04', 'the five questions', 'reverse closure, version window, lockfile join, maintainer neighbourhood, typosquat ring']]
  return <section className="bg-[var(--bg-secondary)] px-4 py-[8rem]"><div className="mx-auto max-w-5xl"><FadeIn><h2 className="font-display text-[2.5rem] font-bold leading-[1.1] tracking-[-0.01em] text-[var(--text-primary)] md:text-[3rem]">from registry to answer in one graph</h2></FadeIn><div className="mt-12 grid grid-cols-1 gap-px overflow-hidden rounded-[8px] border border-[var(--border-default)] bg-[var(--border-default)]">{layers.map(([index, heading, body], layerIndex) => <FadeIn key={index} delay={layerIndex * 0.08}><div className={`cell-hover grid grid-cols-1 items-center bg-[var(--bg-surface)] p-6 md:grid-cols-[64px_1fr_2fr] ${layerIndex === 2 ? 'border-l-2 border-l-[var(--accent)]' : ''}`}><span className="font-mono text-[2.5rem] leading-none text-[var(--text-muted)]">{index}</span><div className="font-sans text-xl font-medium text-[var(--text-primary)]">{heading}</div><p className="font-sans text-sm font-light leading-[1.6] text-[var(--text-secondary)]">{body}</p></div></FadeIn>)}</div></div></section>
}
